<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.pj.sys.dao.SysRoleDao">
    <select id="findObjectById" resultMap="SysRoleMenu">
      select id,name,note from sys_roles where id=#{id} 
    </select>
    <resultMap type="com.cy.pj.common.vo.SysRoleMenu" id="SysRoleMenu">
    <id property="id" column="id"/>
    <collection property="menuIds" select="com.cy.pj.sys.dao.SysRoleMenuDao.findMenuIdsByRoleId"
                    column="id">        
    </collection>
    </resultMap>
    <update id="updateObject"
    parameterType="com.cy.pj.sys.pojo.SysRole">  
       update sys_roles
         set
           name=#{name},
           note=#{note},
           modifiedUser=#{modifiedUser},
           modifiedTime=now()
        where id=#{id}  
    </update>
	<sql id="queryWhereId">
		<!-- 共性 -->
		<where>
			<if test="name!=null and name!=''">
				name like concat("%",#{name},"%")
			</if>
		</where>
	</sql>
	<select id="getRowCount" resultType="int">
		select count(*)
		from sys_roles
		<include refid="queryWhereId" />
	</select>

	<select id="findPageObjects"
		resultType="com.cy.pj.sys.pojo.SysRole">
		select * from sys_roles
		<include refid="queryWhereId" />
		order by createdTime desc
		limit #{startIndex},#{pageSize}
	</select>
	<delete id="deleteObject">
		delete from sys_roles
		where id =#{id}
	</delete>
	<insert id="insertObject"
		parameterType="com.cy.pj.sys.pojo.SysRole" useGeneratedKeys="true"
		keyProperty="id">
		insert into sys_roles
		(id,name,note,createdTime,modifiedTime,
		createdUser,modifiedUser)
		values
		(null,#{name},#{note},now(),now(),
		#{createdUser},#{modifiedUser})

	</insert>
	<select id="findObjects"
		resultType="com.cy.pj.common.vo.CheckBox">
		select id,name
		from sys_roles
	</select>
	
</mapper>